from setuptools import setup

setup(name='shot_eval',
      version='0.1',
      author='Michael Gygli, ETH Zurich',
      packages=['shot_eval'],
      install_requires=['numpy'],
      zip_safe=False)
